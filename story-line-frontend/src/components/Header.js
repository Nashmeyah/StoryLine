import React from "react";

const Header = () => {
  const headStyle = {
    color: "white",
    fontSize: "50px",
  };
  return (
    <div>
      <h1 style={headStyle}>StoryLine Creator</h1>
    </div>
  );
};

export default Header;
